var express = require('express'),
	router = express.Router(),
	User = require('../models/User'),
	Activity = require('../models/Activity');

router.route('/listAct').get(function (req, res) {
	
});

module.exports = router;